#include<iostream>
using namespace std;
int main() {
	int num[5] = { 1,-9,32,76,0 };
	int smallest = INT_MAX;
	for (int i = 0; i < 5; i++) {
		if (num[i] < smallest) {
			smallest = num[i];
		}
	}
	cout << "THE SMALLEST NUMBER IS: " << smallest;
	return 0;
}
